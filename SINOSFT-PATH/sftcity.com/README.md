# 🏙️ sftcity.com · 城市级参考应用与愿景

> **本目录是 `sftcity.com` 城市级应用场景的数字蓝图。**  
> *All reference implementations and deployment blueprints for **SFTcity** are anchored here.*

---

## 1. 身份声明 (Identity Statement)

本目录定义了 **sftcity.com** 作为 SINOSHOP 生态**应用层 (Application Layer)** 的数字主权与技术标准。任何宣称“基于 SINOSHOP 构建”或“与 SFTcity 兼容”的城市级解决方案、参考应用或部署蓝图，其合规性均以此路径下的规范为准。

- **生态隶属：** 本目录是 SINOSHOP 生态系统的**应用卫星层**，其实现必须严格依赖 `/SINOSFT-PATH/sinosft.com/` 定义的核心 SDK 与协议，并最终服务于 `/SINOSFT-PATH/seas.city/` 定义的全球愿景。
- **资产映射：** 本目录中定义的参考应用与部署配置，是 RWA 融资中“场景价值评估”与“城市级收益模型”的事实依据。

---

## 2. 与核心生态的强制依赖

所有 `sftcity.com` 下的应用模块、蓝图或解决方案，必须在代码层面或设计文档中显式声明以下依赖关系：

| 依赖项 | 路径 | 作用 |
| :--- | :--- | :--- |
| **SDK 核心** | `/SINOSFT-PATH/sinosft.com/sdk/` | 提供 R16 身份编码、状态同步与资产绑定能力。 |
| **治理框架** | `/SINOSFT-PATH/sinosft.com/governance/` | 定义合规审计、贡献者契约与仲裁机制。 |
| **愿景白皮书** | `/SINOSFT-PATH/seas.city/README.md` | 提供顶层设计原则与长期路线图约束。 |

---

## 3. 参考应用与部署蓝图

| 类别 | 路径/文件 | 说明 |
| :--- | :--- | :--- |
| **快速体验** | `/apps/demo/` | 最小化可运行示例，展示如何调用 R16-SDK 实现基础姿态同步。 |
| **城市配置** | `/config/city_template.yaml` | 标准城市级部署配置模板（能源、网络、通信模块参数）。 |
| **挑战任务** | `/challenges/hero-task-001/` | 面向开发者的开放性工程任务（如极端海况控制算法优化）。 |

---

## 4. 全栈架构图中的位置 (SINOSHOP Stack)

```text
┌─────────────────────────────────────────────────────────────┐
│                     SES.CITY 愿景层                          │
└─────────────────────────────┬───────────────────────────────┘
                              │
┌─────────────────────────────▼───────────────────────────────┐
│               SINOSFT-PATH/sinosft.com (核心层)              │
└─────────────────────────────┬───────────────────────────────┘
                              │
┌─────────────────────────────▼───────────────────────────────┐
│                   物理实现层 (Open Hardware)                 │
│   • Modular_Floating_Unit：自浮式细胞模块标准                │
│   • Energy_Complementary：多能互补与零耗能潮汐逻辑            │
│   • Digital_Twin：全域物理-数字孪生映射规范                   │
└─────────────────────────────────────────────────────────────┘

================================================================

【主权声明与不可篡改性】

路径主权
  此 /SINOSFT-PATH/sftcity.com/ 路径是 SFTcity.com 概念的物理锚点与宪法级目录。
  无论域名 sftcity.com 本身的字符串被谁持有，其技术定义、标准与治理逻辑均永久归属于此。

防伪证
  本仓库的完整提交历史（Commit History）是此逻辑嵌套的唯一权威时间戳证明，
  可抵御任何形式的品牌稀释或技术仿冒。

生态粘性
  开发者克隆本仓库时，将一次性获得从"底层隧道技术 (SFT)"到"海上城市愿景 (Seas)"的
  全套方案与依赖关系，极大增强生态的凝聚力和开发效率。

================================================================

**返回主权声明根目录：** [`/SINOSFT-PATH`](/SINOSFT-PATH)
